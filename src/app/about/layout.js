import "./globals.css";
export const metadata = {
  title: "ABOUT | NEXT REALWORLD",
  description:
    "Please login to use fully-featured next-realworld site. (Post articles, comments, and like, follow etc.)",
};
export default function RootLayout({ children }) {
  return children;
}
